export const ADMIN_CSS = `
:root{color-scheme:light;--bg:#f4f4f1;--panel:#fff;--panel-soft:#f8f8f6;--text:#171717;--muted:#676761;--line:#d9d9d4;--line-strong:#b7b7b0;--accent:#1d4ed8;--accent-hover:#1e40af;--accent-soft:#e8efff;--good:#166534;--good-bg:#dcfce7;--bad:#991b1b;--bad-bg:#fee2e2;--warn:#854d0e;--warn-bg:#fef3c7;--focus:#2563eb;--shadow:0 18px 45px rgba(17,24,39,.09)}
@media(prefers-color-scheme:dark){:root{color-scheme:dark;--bg:#0c0c0b;--panel:#171716;--panel-soft:#20201e;--text:#f4f4f1;--muted:#aaa9a2;--line:#353532;--line-strong:#55554f;--accent:#7da2ff;--accent-hover:#a5bcff;--accent-soft:#1d2946;--good:#86efac;--good-bg:#143621;--bad:#fca5a5;--bad-bg:#421818;--warn:#fde68a;--warn-bg:#422f12;--focus:#93b4ff;--shadow:0 20px 50px rgba(0,0,0,.34)}}
*{box-sizing:border-box}html{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--text)}body{margin:0;min-height:100dvh}button,input,select,textarea{font:inherit}button{cursor:pointer}button:disabled{cursor:wait;opacity:.58}[hidden]{display:none!important}.skip{position:fixed;top:-70px;left:14px;z-index:20;background:var(--text);color:var(--bg);padding:10px 14px;border-radius:7px}.skip:focus{top:14px}a{color:var(--accent)}:focus-visible{outline:3px solid color-mix(in srgb,var(--focus) 48%,transparent);outline-offset:2px}
.login-shell{min-height:100dvh;display:grid;place-items:center;padding:24px}.login-card{width:min(430px,100%);background:var(--panel);border:1px solid var(--line);border-radius:14px;box-shadow:var(--shadow);padding:30px}.brand{display:flex;align-items:center;gap:11px;margin-bottom:26px}.brand-mark{display:grid;place-items:center;width:38px;height:38px;border-radius:10px;background:var(--text);color:var(--bg);font-weight:800}.brand-name{font-weight:750;letter-spacing:-.015em}.eyebrow{margin:0 0 5px;color:var(--muted);font-size:12px;font-weight:750;letter-spacing:.12em;text-transform:uppercase}h1{margin:0;font-size:29px;line-height:1.18;letter-spacing:-.03em}.login-copy{margin:10px 0 24px;color:var(--muted);line-height:1.65}.field{display:grid;gap:7px;margin-bottom:16px}.field label{font-size:13px;font-weight:700}.field-help{font-size:12px;color:var(--muted)}.control{width:100%;min-height:42px;border:1px solid var(--line-strong);border-radius:8px;background:var(--panel);color:var(--text);padding:9px 11px}.control:focus{border-color:var(--focus)}textarea.control{min-height:150px;resize:vertical;font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.login-error{margin:0 0 16px;padding:10px 12px;border-radius:8px;color:var(--bad);background:var(--bad-bg);font-size:13px}.security-note{margin:18px 0 0;color:var(--muted);font-size:12px;line-height:1.55}
.button{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-height:38px;border:1px solid var(--line-strong);border-radius:8px;background:var(--panel);color:var(--text);padding:8px 12px;font-weight:650;text-decoration:none;transition:background-color .15s,border-color .15s,color .15s}.button:hover{background:var(--panel-soft)}.button.primary{background:var(--accent);border-color:var(--accent);color:#fff}.button.primary:hover{background:var(--accent-hover);border-color:var(--accent-hover)}.button.danger{color:var(--bad)}.button.quiet{border-color:transparent;background:transparent}.button.small{min-height:32px;padding:5px 9px;font-size:12px}.button.full{width:100%}
.shell{width:min(1440px,100%);margin:auto;padding:28px 24px 60px}.topbar{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:22px}.top-actions,.toolbar-actions,.row-actions,.bulk-actions,.dialog-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.subtitle{margin:7px 0 0;color:var(--muted);max-width:72ch}.source-badge{display:inline-flex;margin-top:10px;border-radius:999px;padding:4px 9px;background:var(--panel-soft);border:1px solid var(--line);color:var(--muted);font-size:12px;font-weight:700}.layout{display:grid;gap:16px}.strategy-panel{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:20px;align-items:end;background:var(--panel);border:1px solid var(--line);border-radius:11px;padding:17px 18px}.strategy-panel h2,.panel-head h2{font-size:15px;margin:0}.strategy-panel p{margin:5px 0 0;color:var(--muted);font-size:13px}.strategy-controls{display:flex;align-items:end;gap:9px}.strategy-controls .field{margin:0;min-width:220px}.stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.stat{border:1px solid var(--line);background:var(--panel);border-radius:10px;padding:15px 16px}.stat-label{color:var(--muted);font-size:12px}.stat-value{display:block;margin-top:4px;font-size:25px;line-height:1.15;font-weight:760;font-variant-numeric:tabular-nums}.panel{background:var(--panel);border:1px solid var(--line);border-radius:11px;overflow:hidden}.panel-head{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:14px 16px;border-bottom:1px solid var(--line)}.updated{color:var(--muted);font-size:12px}.bulk-bar{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 16px;border-bottom:1px solid var(--line);background:var(--accent-soft);font-size:13px}.table-wrap{overflow-x:auto}table{width:100%;border-collapse:collapse;min-width:1120px}th,td{text-align:left;padding:11px 12px;border-bottom:1px solid var(--line);vertical-align:middle}th{background:var(--panel-soft);color:var(--muted);font-size:11px;letter-spacing:.05em;text-transform:uppercase}tbody tr:last-child td{border-bottom:0}tbody tr:has(input[type=checkbox]:checked){background:color-mix(in srgb,var(--accent-soft) 50%,transparent)}.check-cell{width:42px;text-align:center}.account{font-weight:680}.account-id{display:block;color:var(--muted);font:11px/1.4 ui-monospace,SFMono-Regular,Menlo,monospace;margin-top:2px}.badge{display:inline-flex;align-items:center;gap:6px;border-radius:999px;padding:3px 8px;font-size:12px;font-weight:680;white-space:nowrap}.badge::before{content:"";width:7px;height:7px;border-radius:50%;background:currentColor}.healthy{color:var(--good);background:var(--good-bg)}.cooling{color:var(--bad);background:var(--bad-bg)}.disabled{color:var(--warn);background:var(--warn-bg)}.number{font-variant-numeric:tabular-nums}.time{white-space:nowrap;color:var(--muted);font-size:12px}.state{padding:48px 20px;text-align:center;color:var(--muted)}.state strong{display:block;color:var(--text);font-size:15px;margin-bottom:5px}.error{color:var(--bad)}.skeleton{height:14px;border-radius:5px;background:var(--panel-soft);animation:pulse 1.2s ease-in-out infinite alternate}.notice{position:fixed;right:20px;bottom:20px;max-width:min(420px,calc(100vw - 40px));z-index:30;border:1px solid var(--line);border-radius:9px;background:var(--panel);box-shadow:var(--shadow);padding:11px 14px;font-size:13px}.notice.error{border-color:color-mix(in srgb,var(--bad) 45%,var(--line));color:var(--bad)}.preview{border:1px solid var(--line);border-radius:9px;background:var(--panel-soft);padding:14px;margin-top:4px}.preview h3{font-size:14px;margin:0 0 10px}.preview-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.preview-stat{border-radius:7px;background:var(--panel);border:1px solid var(--line);padding:9px}.preview-stat span{display:block;color:var(--muted);font-size:11px}.preview-stat strong{display:block;margin-top:3px;font-size:18px}.preview-list{max-height:140px;overflow:auto;margin:10px 0 0;padding-left:20px;color:var(--muted);font-size:12px}.preview-note{margin:10px 0 0;color:var(--warn);font-size:12px}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
dialog{width:min(620px,calc(100vw - 30px));max-height:calc(100dvh - 30px);border:1px solid var(--line);border-radius:12px;background:var(--panel);color:var(--text);box-shadow:var(--shadow);padding:0}dialog::backdrop{background:rgba(0,0,0,.48)}.login-dialog{width:min(420px,calc(100vw - 30px))}.login-dialog .dialog-body p{margin:0;line-height:1.6}.dialog-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;padding:18px 20px;border-bottom:1px solid var(--line)}.dialog-head h2{font-size:18px;margin:0}.dialog-head p{margin:4px 0 0;color:var(--muted);font-size:13px}.dialog-body{padding:20px;overflow:auto}.dialog-actions{justify-content:flex-end;padding:14px 20px;border-top:1px solid var(--line);background:var(--panel-soft)}.inline-warning{padding:10px 12px;border-radius:8px;background:var(--warn-bg);color:var(--warn);font-size:12px;line-height:1.55;margin-bottom:16px}
@keyframes pulse{to{opacity:.42}}@media(prefers-reduced-motion:reduce){.skeleton{animation:none}.button{transition:none}}@media(max-width:820px){.shell{padding:20px 14px 46px}.topbar{display:block}.top-actions{margin-top:16px}.strategy-panel{grid-template-columns:1fr;align-items:start}.strategy-controls{align-items:stretch;flex-direction:column}.strategy-controls .field{min-width:0}.stats{grid-template-columns:repeat(2,minmax(0,1fr))}.panel-head{align-items:flex-start;flex-direction:column}.bulk-bar{align-items:flex-start;flex-direction:column}h1{font-size:25px}}@media(max-width:440px){.stats{grid-template-columns:1fr 1fr}.stat{padding:12px}.stat-value{font-size:21px}.top-actions>.button,.top-actions>form{flex:1}.top-actions form .button{width:100%}.login-card{padding:23px 20px}}
`;

export const ADMIN_JS = `
(()=>{
  const state={snapshot:null,selected:new Set(),noticeTimer:null,importPreviewKey:''};
  const $=selector=>document.querySelector(selector);
  const refs={content:$('#content'),refresh:$('#refresh'),importButton:$('#import-button'),source:$('#source'),updated:$('#updated'),bulk:$('#bulk-bar'),bulkCount:$('#bulk-count'),selectAll:$('#select-all'),strategy:$('#routing-strategy'),restore:$('#restore-environment'),notice:$('#notice'),announce:$('#announce'),importDialog:$('#import-dialog'),importForm:$('#import-form'),importText:$('#import-accounts'),importMode:$('#import-mode'),importPreview:$('#import-preview'),importPreviewStats:$('#import-preview-stats'),importPreviewList:$('#import-preview-list'),importPreviewNote:$('#import-preview-note'),importSubmit:$('#import-submit'),editDialog:$('#edit-dialog'),editForm:$('#edit-form'),editId:$('#edit-account-id'),editLabel:$('#edit-label'),editCookie:$('#edit-cookie'),editSapisid:$('#edit-sapisid'),testDialog:$('#account-test-dialog'),testTitle:$('#account-test-title'),testResult:$('#account-test-result')};
  const labels={healthy:'可用',cooling:'冷却中',disabled:'已停用'};
  const strategyLabels={round_robin:'轮询',priority:'固定优先级',least_used:'最少请求'};
  const issueLabels={auth:'认证失败',rate_limit:'上游限流',transient:'网络或上游异常'};
  const time=value=>value?new Intl.DateTimeFormat('zh-CN',{dateStyle:'short',timeStyle:'medium'}).format(new Date(value)):'—';
  const button=(label,onClick,options={})=>{const el=document.createElement('button');el.type='button';el.className='button small'+(options.danger?' danger':'');el.textContent=label;el.addEventListener('click',onClick);return el};
  const cell=(text,className='')=>{const el=document.createElement('td');el.textContent=text;if(className)el.className=className;return el};
  async function api(path,options={}){const response=await fetch(path,{cache:'no-store',...options,headers:{accept:'application/json',...(options.body?{'content-type':'application/json'}:{}),...(options.headers||{})}});if(response.status===401){location.assign('/admin/login');throw new Error('登录已过期');}let body={};try{body=await response.json()}catch{}if(!response.ok)throw new Error(body.message||body.error||('HTTP '+response.status));return body;}
  function notify(message,isError=false){clearTimeout(state.noticeTimer);refs.notice.textContent=message;refs.notice.className='notice'+(isError?' error':'');refs.notice.hidden=false;refs.announce.textContent=message;state.noticeTimer=setTimeout(()=>{refs.notice.hidden=true},4200)}
  function setPending(element,pending){element.disabled=pending;element.setAttribute('aria-busy',String(pending))}
  function renderSummary(accounts){$('#total').textContent=accounts.length;$('#healthy').textContent=accounts.filter(item=>item.status==='healthy').length;$('#failed').textContent=accounts.filter(item=>item.status==='cooling').length;$('#successes').textContent=accounts.reduce((sum,item)=>sum+item.success_count,0).toLocaleString();}
  function render(snapshot){state.snapshot=snapshot;const accounts=snapshot.accounts||[];state.selected=new Set([...state.selected].filter(id=>accounts.some(item=>item.account_id===id)));renderSummary(accounts);refs.source.textContent=snapshot.configuration_source==='managed'?'后台托管配置':'Worker Secret 配置';refs.strategy.value=snapshot.routing_strategy||'round_robin';refs.restore.hidden=snapshot.configuration_source!=='managed';refs.updated.textContent='更新于 '+new Date().toLocaleTimeString('zh-CN');refs.content.replaceChildren();refs.content.setAttribute('aria-busy','false');if(!accounts.length){const empty=document.createElement('div');empty.className='state';const strong=document.createElement('strong');strong.textContent='尚未配置账号';empty.append(strong,document.createTextNode('点击“批量导入”添加账号，Cookie 只会写入服务端存储。'));refs.content.append(empty);updateBulk();return;}const wrap=document.createElement('div');wrap.className='table-wrap';const table=document.createElement('table');table.setAttribute('aria-label','Gemini 账号状态');const thead=document.createElement('thead');const head=document.createElement('tr');const checkHead=document.createElement('th');checkHead.className='check-cell';const all=document.createElement('input');all.type='checkbox';all.id='select-all';all.setAttribute('aria-label','选择全部账号');all.checked=accounts.length>0&&accounts.every(item=>state.selected.has(item.account_id));all.indeterminate=state.selected.size>0&&!all.checked;all.addEventListener('change',()=>{state.selected=all.checked?new Set(accounts.map(item=>item.account_id)):new Set();render(snapshot)});checkHead.append(all);head.append(checkHead);for(const title of ['账号','状态','请求 / 成功','认证失败','最后成功','最后失败','Cookie 刷新','操作']){const th=document.createElement('th');th.textContent=title;head.append(th)}thead.append(head);const tbody=document.createElement('tbody');accounts.forEach((row,index)=>{const tr=document.createElement('tr');const checkCell=document.createElement('td');checkCell.className='check-cell';const check=document.createElement('input');check.type='checkbox';check.checked=state.selected.has(row.account_id);check.setAttribute('aria-label','选择 '+row.label);check.addEventListener('change',()=>{check.checked?state.selected.add(row.account_id):state.selected.delete(row.account_id);updateBulk()});checkCell.append(check);tr.append(checkCell);const account=cell('');const name=document.createElement('span');name.className='account';name.textContent=(index+1)+'. '+row.label;const id=document.createElement('span');id.className='account-id';id.textContent=row.account_id;account.append(name,id);const status=cell('');const badge=document.createElement('span');badge.className='badge '+row.status;badge.textContent=labels[row.status]||row.status;status.append(badge);const actions=cell('','row-actions');actions.append(button('测试',()=>testAccount(row)));actions.append(button('编辑',()=>openEdit(row)));actions.append(button(row.status==='healthy'?'停用':'启用',()=>singleAction(row.account_id,row.status==='healthy'?'disable':'enable'),{danger:row.status==='healthy'}));actions.append(button('重置',()=>singleAction(row.account_id,'reset')));const up=button('上移',()=>moveAccount(index,-1));up.disabled=index===0;const down=button('下移',()=>moveAccount(index,1));down.disabled=index===accounts.length-1;actions.append(up,down);tr.append(account,status,cell(row.request_count.toLocaleString()+' / '+row.success_count.toLocaleString(),'number'),cell(row.auth_failure_count.toLocaleString(),'number'),cell(time(row.last_success_at_ms),'time'),cell(time(row.last_error_at_ms),'time'),cell(time(row.last_cookie_refresh_at_ms),'time'),actions);tbody.append(tr)});table.append(thead,tbody);wrap.append(table);refs.content.append(wrap);updateBulk();}
  function updateBulk(){const count=state.selected.size;refs.bulk.hidden=count===0;refs.bulkCount.textContent='已选择 '+count+' 个账号';}
  async function load(){setPending(refs.refresh,true);try{render(await api('/admin/api/accounts'))}catch(error){refs.content.setAttribute('aria-busy','false');refs.content.replaceChildren();const box=document.createElement('div');box.className='state error';const strong=document.createElement('strong');strong.textContent='无法加载账号状态';const detail=document.createElement('span');detail.textContent=String(error);const retry=button('重试',load);box.append(strong,detail,document.createElement('br'),retry);refs.content.append(box)}finally{setPending(refs.refresh,false)}}
  async function singleAction(accountId,action){try{render(await api('/admin/api/accounts',{method:'PATCH',body:JSON.stringify({account_id:accountId,action})}));notify('账号状态已更新')}catch(error){notify(String(error),true)}}
  async function testAccount(row){refs.testTitle.textContent='正在测试 '+row.label;refs.testResult.textContent='正在请求 Gemini 登录页验证认证状态…';refs.testDialog.showModal();try{const result=await api('/admin/api/accounts/test',{method:'POST',body:JSON.stringify({account_id:row.account_id})});refs.testTitle.textContent=(result.ok?'账号可用 · ':'账号异常 · ')+row.label;refs.testResult.textContent=result.ok?'认证成功，延迟 '+result.latency_ms+' ms。':'检测结果：'+(issueLabels[result.issue]||'未知异常')+'，延迟 '+result.latency_ms+' ms'+(result.upstream_status?'，HTTP '+result.upstream_status:'')+'。'}catch(error){refs.testTitle.textContent='测试失败 · '+row.label;refs.testResult.textContent=String(error)}}
  async function moveAccount(index,direction){if(!state.snapshot)return;const ids=state.snapshot.accounts.map(item=>item.account_id);const target=index+direction;if(target<0||target>=ids.length)return;[ids[index],ids[target]]=[ids[target],ids[index]];try{render(await api('/admin/api/accounts/reorder',{method:'POST',body:JSON.stringify({account_ids:ids})}));notify('账号优先级顺序已更新')}catch(error){notify(String(error),true)}}
  async function bulkAction(action){const accountIds=[...state.selected];if(!accountIds.length)return;if(action==='delete'&&!confirm('确定删除选中的 '+accountIds.length+' 个账号？该操作不会修改 Worker Secret。'))return;try{render(await api('/admin/api/accounts/bulk',{method:'POST',body:JSON.stringify({account_ids:accountIds,action})}));state.selected.clear();updateBulk();notify(action==='delete'?'账号已删除':'批量操作已完成')}catch(error){notify(String(error),true)}}
  function openEdit(row){refs.editId.value=row.account_id;refs.editLabel.value=row.label;refs.editCookie.value='';refs.editSapisid.value='';refs.editDialog.showModal();refs.editLabel.focus()}
  refs.editForm.addEventListener('submit',async event=>{event.preventDefault();const submit=refs.editForm.querySelector('[type=submit]');setPending(submit,true);const body={account_id:refs.editId.value,label:refs.editLabel.value.trim()};if(refs.editCookie.value.trim())body.cookie=refs.editCookie.value.trim();if(refs.editSapisid.value.trim())body.sapisid=refs.editSapisid.value.trim();try{render(await api('/admin/api/accounts/edit',{method:'POST',body:JSON.stringify(body)}));refs.editCookie.value='';refs.editSapisid.value='';refs.editDialog.close();notify('账号信息已更新')}catch(error){notify(String(error),true)}finally{setPending(submit,false)}});
  function clearImportPreview(){state.importPreviewKey='';refs.importPreview.hidden=true;refs.importPreviewStats.replaceChildren();refs.importPreviewList.replaceChildren();refs.importPreviewNote.textContent='';refs.importSubmit.textContent='预检导入'}
  function renderImportPreview(preview){refs.importPreviewStats.replaceChildren();for(const [label,value] of [['输入',preview.total_input],['新增',preview.add_count],['更新',preview.update_count],['删除',preview.remove_count]]){const box=document.createElement('div');box.className='preview-stat';const name=document.createElement('span');name.textContent=label;const count=document.createElement('strong');count.textContent=String(value);box.append(name,count);refs.importPreviewStats.append(box)}refs.importPreviewList.replaceChildren();for(const account of preview.accounts){const item=document.createElement('li');item.textContent=(account.action==='add'?'新增：':'更新：')+account.label;refs.importPreviewList.append(item)}refs.importPreviewNote.textContent=preview.duplicate_count?'发现 '+preview.duplicate_count+' 个重复账号；同一账号将使用最后一条输入。':'未发现重复账号。';refs.importPreview.hidden=false;refs.importSubmit.textContent='确认写入'}
  refs.importForm.addEventListener('submit',async event=>{event.preventDefault();let accounts;try{accounts=JSON.parse(refs.importText.value)}catch{clearImportPreview();notify('导入内容不是有效 JSON',true);return}const payload={mode:refs.importMode.value,accounts};const key=JSON.stringify(payload);setPending(refs.importSubmit,true);try{if(state.importPreviewKey!==key){const preview=await api('/admin/api/accounts/import/preview',{method:'POST',body:key});renderImportPreview(preview);state.importPreviewKey=key;return}render(await api('/admin/api/accounts/import',{method:'POST',body:key}));refs.importText.value='';clearImportPreview();refs.importDialog.close();notify('账号已安全导入，响应未返回 Cookie')}catch(error){clearImportPreview();notify(String(error),true)}finally{setPending(refs.importSubmit,false)}});
  refs.strategy.addEventListener('change',async()=>{setPending(refs.strategy,true);try{const snapshot=await api('/admin/api/settings',{method:'PATCH',body:JSON.stringify({routing_strategy:refs.strategy.value})});render(snapshot);notify('路由策略已切换为'+strategyLabels[snapshot.routing_strategy])}catch(error){notify(String(error),true);if(state.snapshot)refs.strategy.value=state.snapshot.routing_strategy}finally{setPending(refs.strategy,false)}});
  refs.restore.addEventListener('click',async()=>{if(!confirm('确定停止后台托管并恢复 Worker Secret 中的账号列表？后台导入的账号将从活动池移除。'))return;setPending(refs.restore,true);try{render(await api('/admin/api/configuration/restore',{method:'POST',body:'{}'}));notify('已恢复 Worker Secret 配置')}catch(error){notify(String(error),true)}finally{setPending(refs.restore,false)}});
  refs.importText.addEventListener('input',clearImportPreview);refs.importMode.addEventListener('change',clearImportPreview);refs.refresh.addEventListener('click',load);refs.importButton.addEventListener('click',()=>{clearImportPreview();refs.importDialog.showModal();refs.importText.focus()});document.querySelectorAll('[data-close-dialog]').forEach(element=>element.addEventListener('click',()=>element.closest('dialog').close()));document.querySelectorAll('[data-bulk-action]').forEach(element=>element.addEventListener('click',()=>bulkAction(element.dataset.bulkAction)));load();
})();
`;

export const ADMIN_LOGIN_JS = `
(()=>{
  const form=document.querySelector('#login-form');
  const password=document.querySelector('#password');
  const submit=document.querySelector('#login-submit');
  const dialog=document.querySelector('#login-error-dialog');
  const message=document.querySelector('#login-error-message');
  const close=document.querySelector('#login-error-close');
  if(!form||!password||!submit||!dialog||!message||!close)return;
  const showError=text=>{message.textContent=text;if(!dialog.open)dialog.showModal()};
  close.addEventListener('click',()=>dialog.close());
  dialog.addEventListener('close',()=>{password.focus();password.select()});
  form.addEventListener('submit',async event=>{
    if(!form.reportValidity())return;
    event.preventDefault();
    submit.disabled=true;submit.setAttribute('aria-busy','true');submit.textContent='正在登录…';
    try{
      const response=await fetch(form.action,{method:'POST',headers:{accept:'application/json'},body:new URLSearchParams(new FormData(form)),credentials:'same-origin',cache:'no-store'});
      if(response.ok){password.value='';location.assign('/admin');return}
      let details={};try{details=await response.json()}catch{}
      if(response.status===401)showError('用户名或密码不正确。');
      else if(response.status===429)showError('尝试次数过多，请在 '+(details.retry_after_seconds||'一段时间')+' 秒后重试。');
      else if(response.status===403)showError('登录请求被同源安全检查拒绝，请直接从当前 Worker 地址重新打开登录页。');
      else showError('登录暂时失败（HTTP '+response.status+'），请稍后重试。');
    }catch{showError('无法连接到登录服务，请检查网络后重试。')}
    finally{submit.disabled=false;submit.setAttribute('aria-busy','false');submit.textContent='登录管理后台'}
  });
})();
`;

export function adminLoginPage(errorMessage: string | false = false): string {
	return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><title>登录 · web2gem-plus</title><link rel="stylesheet" href="/admin/assets/admin.css"><script defer src="/admin/assets/login.js"></script></head>
<body><main class="login-shell"><section class="login-card" aria-labelledby="login-title"><div class="brand"><span class="brand-mark" aria-hidden="true">W</span><span class="brand-name">web2gem-plus</span></div><p class="eyebrow">管理控制台</p><h1 id="login-title">登录账号池后台</h1><p class="login-copy">使用部署时配置的管理员凭据继续。用户名不会在页面中预填或公开。</p>${errorMessage ? `<p class="login-error" role="alert">${errorMessage}</p>` : ""}<form id="login-form" method="post" action="/admin/login"><div class="field"><label for="username">用户名</label><input class="control" id="username" name="username" required maxlength="120" autocomplete="username" autofocus></div><div class="field"><label for="password">管理员密码</label><input class="control" id="password" name="password" type="password" required maxlength="4096" autocomplete="current-password"></div><button id="login-submit" class="button primary full" type="submit">登录管理后台</button></form><p class="security-note">连续失败会触发按客户端 IP 的指数退避。会话保存在 HttpOnly Cookie 中，12 小时后自动失效。</p></section></main><dialog id="login-error-dialog" class="login-dialog" aria-labelledby="login-error-title"><div class="dialog-head"><div><h2 id="login-error-title">登录失败</h2><p>未创建管理会话</p></div></div><div class="dialog-body"><p id="login-error-message" role="alert">用户名或密码不正确。</p></div><div class="dialog-actions"><button id="login-error-close" class="button primary" type="button">返回重试</button></div></dialog></body></html>`;
}

export function adminDashboardPage(): string {
	return `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><title>Gemini 账号池 · web2gem-plus</title><link rel="stylesheet" href="/admin/assets/admin.css"><script defer src="/admin/assets/admin.js"></script></head>
<body><a class="skip" href="#main">跳到主要内容</a><main id="main" class="shell"><header class="topbar"><div><p class="eyebrow">web2gem-plus / admin</p><h1>Gemini 账号池</h1><p class="subtitle">管理账号状态、路由策略和服务端凭据。Cookie 与 SAPISID 只允许写入，页面和读取 API 永不返回。</p><span id="source" class="source-badge">正在读取配置来源</span></div><div class="top-actions"><button id="import-button" class="button primary" type="button">批量导入</button><button id="refresh" class="button" type="button">刷新状态</button><form method="post" action="/admin/logout"><button class="button quiet" type="submit">退出登录</button></form></div></header><div class="layout"><section class="strategy-panel" aria-labelledby="strategy-title"><div><h2 id="strategy-title">请求路由策略</h2><p>轮询平均分配；固定优先级始终选择列表中首个可用账号；最少请求优先选择累计请求较少的账号。</p></div><div class="strategy-controls"><div class="field"><label for="routing-strategy">当前策略</label><select class="control" id="routing-strategy"><option value="round_robin">轮询</option><option value="priority">固定优先级</option><option value="least_used">最少请求</option></select></div><button id="restore-environment" class="button danger" type="button" hidden>恢复 Secret 配置</button></div></section><section class="stats" aria-label="账号池概览"><div class="stat"><span class="stat-label">账号总数</span><strong class="stat-value" id="total">—</strong></div><div class="stat"><span class="stat-label">可用</span><strong class="stat-value" id="healthy">—</strong></div><div class="stat"><span class="stat-label">冷却中</span><strong class="stat-value" id="failed">—</strong></div><div class="stat"><span class="stat-label">累计成功</span><strong class="stat-value" id="successes">—</strong></div></section><section class="panel" aria-labelledby="accounts-title"><div class="panel-head"><h2 id="accounts-title">账号明细</h2><span id="updated" class="updated"></span></div><div id="bulk-bar" class="bulk-bar" hidden><strong id="bulk-count">已选择 0 个账号</strong><div class="bulk-actions"><button class="button small" data-bulk-action="enable" type="button">启用</button><button class="button small" data-bulk-action="disable" type="button">停用</button><button class="button small" data-bulk-action="reset" type="button">重置状态</button><button class="button small danger" data-bulk-action="delete" type="button">删除</button></div></div><div id="content" class="table-wrap" aria-busy="true"><table aria-label="加载账号状态"><tbody><tr><td><div class="skeleton"></div></td></tr><tr><td><div class="skeleton"></div></td></tr></tbody></table></div></section></div><div id="notice" class="notice" hidden></div><div id="announce" class="sr-only" aria-live="polite"></div></main>
<dialog id="import-dialog" aria-labelledby="import-title"><form id="import-form"><div class="dialog-head"><div><h2 id="import-title">批量导入账号</h2><p>先预检变更，再确认写入；预检结果不包含任何凭据。</p></div><button class="button quiet small" type="button" data-close-dialog aria-label="关闭">关闭</button></div><div class="dialog-body"><p class="inline-warning">导入内容仅写入 Durable Object。成功响应和后续查询都不会包含 Cookie 或 SAPISID。</p><div class="field"><label for="import-mode">导入方式</label><select class="control" id="import-mode"><option value="append">追加并更新同账号</option><option value="replace">替换后台托管列表</option></select></div><div class="field"><label for="import-accounts">账号 JSON</label><textarea class="control" id="import-accounts" required spellcheck="false" autocomplete="off" placeholder='[{"name":"主账号","cookie":"__Secure-1PSID=...; __Secure-1PSIDTS=..."}]'></textarea><span class="field-help">最多 100 个账号，单次请求最多 4 MiB。</span></div><section id="import-preview" class="preview" aria-labelledby="import-preview-title" hidden><h3 id="import-preview-title">预检结果</h3><div id="import-preview-stats" class="preview-grid"></div><ul id="import-preview-list" class="preview-list"></ul><p id="import-preview-note" class="preview-note"></p></section></div><div class="dialog-actions"><button class="button" type="button" data-close-dialog>取消</button><button id="import-submit" class="button primary" type="submit">预检导入</button></div></form></dialog>
<dialog id="edit-dialog" aria-labelledby="edit-title"><form id="edit-form"><div class="dialog-head"><div><h2 id="edit-title">修改账号</h2><p>凭据输入框始终为空；留空表示保留服务端现有值。</p></div><button class="button quiet small" type="button" data-close-dialog aria-label="关闭">关闭</button></div><div class="dialog-body"><input id="edit-account-id" type="hidden"><div class="field"><label for="edit-label">账号名称</label><input class="control" id="edit-label" required maxlength="120" autocomplete="off"></div><div class="field"><label for="edit-cookie">替换 Cookie（可选）</label><textarea class="control" id="edit-cookie" maxlength="1048576" spellcheck="false" autocomplete="off" placeholder="留空以保留当前 Cookie"></textarea><span class="field-help">当前 Cookie 不会加载到此表单，也无法通过 API 读取。</span></div><div class="field"><label for="edit-sapisid">替换 SAPISID（可选）</label><input class="control" id="edit-sapisid" maxlength="4096" spellcheck="false" autocomplete="off" placeholder="留空以保留当前 SAPISID"><span class="field-help">当前 SAPISID 同样只写不读。Cookie 已包含 SAPISID 时通常无需单独填写。</span></div></div><div class="dialog-actions"><button class="button" type="button" data-close-dialog>取消</button><button class="button primary" type="submit">保存修改</button></div></form></dialog><dialog id="account-test-dialog" class="login-dialog" aria-labelledby="account-test-title"><div class="dialog-head"><div><h2 id="account-test-title">账号测试</h2><p>只验证认证状态，不发送模型请求。</p></div></div><div class="dialog-body"><p id="account-test-result" role="status">正在测试…</p></div><div class="dialog-actions"><button class="button primary" type="button" data-close-dialog>关闭</button></div></dialog></body></html>`;
}
